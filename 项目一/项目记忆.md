明白。这个对话框以后就只围绕一件事：

项目一：大模型微调 + 偏好对齐项目落地

目标不是学工具本身，而是把项目做成能写简历、能面试讲、能复现的完整项目。

当前项目主线：

电商客服问答场景
  -> SFT 指令微调
  -> LoRA / QLoRA 参数高效训练
  -> DPO 偏好对齐
  -> 训练前后评估
  -> badcase 分析
  -> 简历项目包装

当前阶段：

阶段 2：JD 电商客服 SFT baseline 已切换到 Qwen2.5-1.5B Base，并完成 LoRA rank 消融实验。

当前已完成：

1. JDDC 原始客服数据已清洗为 LLaMA-Factory 可训练数据：
   - 数据集名：ecommerce_customer_sft
   - 样本数：5773
   - 格式：Alpaca instruction/input/output/system/history
   - history：最多保留 4 轮历史对话

2. 当前主线实验设置：
   - 模型：Qwen2.5-1.5B
   - 训练：LoRA SFT
   - 数据：ecommerce_customer_sft 全量 5773 条
   - epoch：2
   - cutoff_len：512
   - batch_size：1
   - gradient_accumulation：8
   - learning_rate：1e-4
   - LoRA dropout：0.05
   - LoRA target：all

3. 当前 baseline：
   - baseline 名称：baseline_r4_all
   - LoRA rank：4
   - LoRA alpha：16
   - eval_loss：约 1.7822
   - ppl：约 5.9432
   - train_loss：约 2.1194
   - 训练目录：LLaMA-Factory/saves/Qwen2.5-1.5B/lora/train_2026-04-25-00-31-Qwen-1.5B-sft-r4-all

4. 已完成的 rank 消融：
   - r=4, alpha=16
   - r=8, alpha=16
   - r=16, alpha=32
   - r=32, alpha=64
   - 统一设置：target=all, epoch=2, lr=1e-4

5. 已新增数据 v2：
   - 数据集名：ecommerce_customer_sft_v2
   - 样本数：2529
   - 来源：从 v1 的 5773 条中过滤保留 1161 条，并补充 1368 条 SOP 场景扩展样本
   - 目标：降低寒暄、收尾、等待查询、评价/打赏、短确认等模板样本占比
   - 精确重复 output 比例：从 v1 约 18.86% 降到 v2 约 0.79%
   - 构建脚本：scripts/build_ecommerce_sft_v2.py
   - 处理报告：项目一/数据处理/ecommerce_customer_sft_v2_report.md

当前判断：

1. `r=4` 作为当前轻量 baseline。
2. `r=8` 与 `r=4` 差距不大。
3. `r=16 / r=32` 的 eval_loss 更低，但 adapter 更大、训练成本更高。
4. `r=4 / r=8 / r=16 / r=32` 均出现重复句式，主要原因是 v1 数据中高频客服模板话术占比过高，再叠加 Qwen2.5-1.5B Base 的聊天停止行为弱。
5. 下一步优先用 `ecommerce_customer_sft_v2` 跑一个 r=4 对照实验，确认重复输出是否缓解，再决定是否继续扩充 v2 或引入 Instruct 对照。

下一步项目路线：

1. 固定 Baseline-raw：Qwen2.5-1.5B 裸模型
2. 固定 Baseline-sft：Qwen2.5-1.5B + LoRA(r=4, target=all)
3. 新增 v2 数据 r=4 对照：Qwen2.5-1.5B + LoRA(r=4, target=all) + ecommerce_customer_sft_v2
4. 用固定评测集完成 v1-r4 vs v2-r4 对比，重点观察重复输出、业务解决能力和回答完整性
5. 如果 v2 缓解复读，再视结果扩充到 3000-5000 条更高质量业务解决型样本
6. 如果 v2 仍复读严重，增加 Qwen2.5-1.5B-Instruct + LoRA 对照
7. 记录 eval_loss / ppl、BLEU / ROUGE、人工评分、A/B 胜率、显存和耗时
8. 做 badcase 分类：胡编、答非所问、缺少操作步骤、语气差、上下文不一致、重复输出
9. 后续构造 chosen/rejected 偏好数据并跑 DPO
10. 整理 README、实验表、badcase、简历描述

以后你问这个对话框，我就按这个项目推进，不再散。
